import type { Config } from "tailwindcss";

export default {
  theme: {
    extend: {
      fontFamily: {
        "google-sans-flex": ["Google Sans Flex", "sans-serif"],
      },
    },
  },
} satisfies Config;
